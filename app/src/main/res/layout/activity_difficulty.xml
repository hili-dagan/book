<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bag2"
    tools:context=".difficultyActivity">

    <!-- שכבת ה-Fade שביקשת (הופכת ללבן) -->
    <View
        android:id="@+id/fade_overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#FFFFFF"
        android:visibility="gone"
        android:elevation="100dp" />

    <TextView
        android:id="@+id/title_difficulty"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="168dp"
        android:fontFamily="sans-serif-black"
        android:shadowColor="#FF000000"
        android:shadowDx="2"
        android:shadowDy="2"
        android:shadowRadius="4"
        android:text="Choose difficulty"
        android:textColor="#CCC5C5"
        android:textSize="35sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.496"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <RadioGroup
        android:id="@+id/radio_group"
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/title_difficulty">

        <RadioButton
            android:id="@+id/static_rb"
            android:layout_width="match_parent"
            android:layout_height="80dp"
            android:button="@drawable/custom_radiobutton"
            android:buttonTint="@null"
            app:useMaterialThemeColors="false"
            android:paddingStart="10dp"
            android:text="Easy- Static Target"
            android:textColor="#DCE2E8"
            android:textSize="20sp" />
        <RadioButton
            android:id="@+id/moving_rb"
            android:layout_width="match_parent"
            android:layout_height="80dp"
            android:button="@drawable/custom_radiobutton"
            android:buttonTint="@null"
            android:paddingStart="15dp"
            android:text="Hard- Dynamic Target"
            android:textColor="#DCE2E8"
            android:textSize="20sp"
            app:useMaterialThemeColors="false" />
    </RadioGroup>
</androidx.constraintlayout.widget.ConstraintLayout>